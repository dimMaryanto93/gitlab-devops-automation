apiVersion: v1
kind: PersistentVolume
metadata:
  name: elk-data-pv
spec:
  hostPath:
    path: /var/lib/elk-data/stored
    type: DirectoryOrCreate
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 15Gi
