- name: get fact all
  hosts: ["all"]
  tasks:
    - debug:
        msg: "OS distribution is {{ ansible_facts['distribution'] }}"
    - debug:
        msg: "OS family is {{ ansible_facts['os_family'] }}"