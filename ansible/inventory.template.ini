gitlab_host             ansible_port=${CI_ANSIBLE_PORT} ansible_user=${CI_ANSIBLE_USER} ansible_host=${CI_ANSIBLE_HOST}
gitlab-runner01_host    ansible_port=${CI_ANSIBLE_PORT} ansible_user=${CI_ANSIBLE_USER} ansible_host=${CI_ANSIBLE_HOST}
nexus-oss_host          ansible_port=${CI_ANSIBLE_PORT} ansible_user=${CI_ANSIBLE_USER} ansible_host=${CI_ANSIBLE_HOST}
sonarqube_host          ansible_port=${CI_ANSIBLE_PORT} ansible_user=${CI_ANSIBLE_USER} ansible_host=${CI_ANSIBLE_HOST}
skywalking_host         ansible_port=${CI_ANSIBLE_PORT} ansible_user=${CI_ANSIBLE_USER} ansible_host=${CI_ANSIBLE_HOST}
jmeter_host             ansible_port=${CI_ANSIBLE_PORT} ansible_user=${CI_ANSIBLE_USER} ansible_host=${CI_ANSIBLE_HOST}

[gitlab]
gitlab_host

[gitlab-runner]
gitlab-runner01_host

[nexus-oss]
nexus-oss_host

[sonarqube]
sonarqube_host

[jmeter]
jmeter_host

[skywalking]
skywalking_host